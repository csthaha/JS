function information(name, sex, money, birthday, color, likes, follow, age) {

    return `${name} ${sex} ${money} ${birthday} ${color} ${likes} ${follow} ${age}`
}

let user = { name: '', sex:'', money:'', birthday:'', color:'', likes:[], follow:'', age: 16}

information(user);